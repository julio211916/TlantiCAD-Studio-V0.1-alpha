// Generated by Copilot
import React from 'react';
import { AlertTriangle, LoaderCircle } from 'lucide-react';
import { cn } from '../../lib/utils';
import { ThemeMode } from '../../types';

interface IndicatorProps {
  themeMode: ThemeMode;
}

export function TlantiOhifLoadingIndicator({
  themeMode,
  label = 'Cargando estudio DICOM',
}: IndicatorProps & { label?: string }) {
  return (
    <div className="absolute inset-0 flex items-center justify-center pointer-events-none">
      <div
        className={cn(
          'flex items-center gap-3 rounded-2xl border px-6 py-4 shadow-xl',
          themeMode === 'dark'
            ? 'bg-surface-raised border-border text-text-primary'
            : 'bg-surface border-border text-text-primary',
        )}
      >
        <LoaderCircle className="animate-spin text-[#FA93FA]" size={18} />
        <div>
          <p className="text-[10px] font-mono uppercase text-text-secondary tabular-nums">
            TlantiCAD · DICOM
          </p>
          <p className="text-sm font-medium">{label}</p>
        </div>
      </div>
    </div>
  );
}

export function TlantiOhifErrorIndicator({
  themeMode,
  details,
}: IndicatorProps & { details: string }) {
  return (
    <div className="absolute inset-0 flex items-center justify-center p-8">
      <div
        className={cn(
          'max-w-md rounded-3xl border px-6 py-5 shadow-xl',
          themeMode === 'dark'
            ? 'bg-surface-raised border-red-500/30 text-text-primary'
            : 'bg-surface border-red-500/30 text-text-primary',
        )}
      >
        <div className="mb-3 flex items-center gap-3 text-red-400">
          <AlertTriangle size={20} />
          <span className="font-mono text-[11px] uppercase tabular-nums">
            Error clínico
          </span>
        </div>
        <p className="mb-1 text-lg font-semibold">No se pudo abrir el estudio</p>
        <p className="text-sm leading-relaxed text-text-secondary">{details}</p>
      </div>
    </div>
  );
}
