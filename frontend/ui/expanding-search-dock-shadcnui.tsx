// Generated by Copilot
import { AnimatePresence, motion } from 'framer-motion';
import { Search, X } from 'lucide-react';
import { useState } from 'react';

import { cn } from '@/lib/utils';

type ExpandingSearchDockProps = {
  onSearch?: (query: string) => void;
  placeholder?: string;
  className?: string;
};

export function ExpandingSearchDock({
  onSearch,
  placeholder = 'Search...',
  className,
}: ExpandingSearchDockProps) {
  const [isExpanded, setIsExpanded] = useState(false);
  const [query, setQuery] = useState('');

  const handleExpand = () => {
    setIsExpanded(true);
  };

  const handleCollapse = () => {
    setIsExpanded(false);
    setQuery('');
    onSearch?.('');
  };

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    onSearch?.(query.trim());
  };

  return (
    <div className={cn('relative', className)}>
      <AnimatePresence mode="wait" initial={false}>
        {!isExpanded ? (
          <motion.button
            key="icon"
            initial={{ scale: 0.96, opacity: 0 }}
            animate={{ scale: 1, opacity: 1 }}
            exit={{ scale: 0.96, opacity: 0 }}
            transition={{ duration: 0.18, ease: 'easeOut' }}
            onClick={handleExpand}
            aria-label="Search cases and settings"
            className="flex size-11 items-center justify-center rounded-full border border-border bg-card text-text-secondary transition-colors hover:bg-muted hover:text-text-primary"
          >
            <Search className="size-4" />
          </motion.button>
        ) : (
          <motion.form
            key="input"
            initial={{ width: 48, opacity: 0 }}
            animate={{ width: 320, opacity: 1 }}
            exit={{ width: 48, opacity: 0 }}
            transition={{ type: 'spring', stiffness: 300, damping: 30 }}
            onSubmit={handleSubmit}
            className="relative"
          >
            <div className="relative flex items-center gap-2 overflow-hidden rounded-full border border-border bg-card px-1">
              <div className="ml-3 text-text-secondary">
                <Search className="size-4" />
              </div>
              <input
                type="text"
                value={query}
                onChange={(event) => {
                  const nextValue = event.target.value;
                  setQuery(nextValue);
                  onSearch?.(nextValue.trim());
                }}
                placeholder={placeholder}
                autoFocus
                className="h-11 flex-1 bg-transparent pr-2 text-sm text-text-primary outline-none placeholder:text-muted-foreground"
              />
              <motion.button
                type="button"
                onClick={handleCollapse}
                aria-label="Clear search"
                initial={{ scale: 0.96, opacity: 0 }}
                animate={{ scale: 1, opacity: 1 }}
                whileTap={{ scale: 0.96 }}
                transition={{ duration: 0.16, ease: 'easeOut' }}
                className="mr-1 flex size-8 items-center justify-center rounded-full text-text-secondary transition-colors hover:bg-muted hover:text-text-primary"
              >
                <X className="size-4" />
              </motion.button>
            </div>
          </motion.form>
        )}
      </AnimatePresence>
    </div>
  );
}
