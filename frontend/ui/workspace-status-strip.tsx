// Generated by Copilot
import React from 'react'

import { Badge } from '@/components/ui/badge'
import { cn } from '@/lib/utils'
import type { WorkspaceStatusItem } from '@/lib/workspace-shell'

const toneClasses: Record<WorkspaceStatusItem['tone'], string> = {
  neutral: 'border-border bg-card text-text-primary',
  info: 'border-sky-500/35 bg-sky-500/10 text-sky-300',
  success: 'border-emerald-500/35 bg-emerald-500/10 text-emerald-300',
  warning: 'border-amber-500/35 bg-amber-500/10 text-amber-300',
  danger: 'border-rose-500/35 bg-rose-500/10 text-rose-300',
  accent: 'border-fuchsia-500/35 bg-fuchsia-500/10 text-fuchsia-300',
}

interface WorkspaceStatusStripProps {
  items: WorkspaceStatusItem[]
  className?: string
}

export function WorkspaceStatusStrip({ items, className }: WorkspaceStatusStripProps) {
  return (
    <div className={cn('flex flex-wrap gap-2', className)}>
      {items.map((item) => (
        <Badge key={item.id} variant="outline" className={cn('border px-2.5 py-1 text-[11px] font-medium', toneClasses[item.tone])}>
          {item.label}
        </Badge>
      ))}
    </div>
  )
}
