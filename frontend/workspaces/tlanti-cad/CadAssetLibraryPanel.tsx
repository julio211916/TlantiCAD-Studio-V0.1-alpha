// Generated by Copilot
import React from 'react';

import { type PublicAssetLibraryActionMode, PublicAssetLibraryBrowser } from '@/components/asset-library/PublicAssetLibraryBrowser';
import { CadResponsivePanel } from '@/components/cad/CadResponsivePanel';
import type { PublicAssetLibraryItem } from '@/lib/public-asset-library';

interface CadAssetLibraryPanelProps {
  compact: boolean;
  moduleId?: string;
  open: boolean;
  onOpenChange: (open: boolean) => void;
  onAction: (asset: PublicAssetLibraryItem, mode: PublicAssetLibraryActionMode) => Promise<void> | void;
  onBatchAction?: (assets: PublicAssetLibraryItem[], mode: PublicAssetLibraryActionMode) => Promise<void> | void;
}

export function CadAssetLibraryPanel({ compact, moduleId, open, onOpenChange, onAction, onBatchAction }: CadAssetLibraryPanelProps) {
  void moduleId;
  return (
    <CadResponsivePanel
      title="Asset Library"
      compact={compact}
      open={open}
      onOpenChange={onOpenChange}
      desktopClassName="left-6 top-24 bottom-24 w-[30rem]"
      contentClassName="p-4"
    >
      <PublicAssetLibraryBrowser
        surface="cad"
        useActionLabel="Insert into CAD"
        description="Scan every bundled public asset and insert compatible images or models directly into the CAD canvas."
        onAction={onAction}
        onBatchAction={onBatchAction}
      />
    </CadResponsivePanel>
  );
}
